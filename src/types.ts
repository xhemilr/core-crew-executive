/**
 * What the quick-start bar hands over to the quote form: positions in the
 * quick-start lists, so the text follows whichever language is showing.
 */
export type Selection = {
  type: number
  level: number
  count: number
  when: number
}
